<nav class="navbar navbar-expand-lg bg-body-light border-bottom sticky-top d-flex flex-column justify-content-between">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><img src="/images/logo_image.png" alt="logo" class="logo_image" />
      <span class="logo_text">Nextify</span></a>
    <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse bg-white" id="navbarNav">
      <div class="navbar-nav">
        <a class="nav-link" href="/">Home</a>
        <!-- <a class="nav-link" href="/listings">All Listings</a> -->
        <a class="nav-link" href="/listings/new">Create a New</a>
        <%if(currUser){%>
        <a class="nav-link" href="/listings/<%=currUser._id%>/<%=currUser.username%>">See Your Listings</a>
        <%}%>
        <a class="nav-link" href="/">Profile</a>
      </div>
      <div class="navbar-nav ms-auto">
        <%if(!currUser){%>
        <a class="nav-link" href="/signup">SignUp</a>
        <a class="nav-link" href="/login">Login</a>
        <%} else {%>
        <a class="nav-link" href="/logout">Logout</a>
        <%}%>
      </div>
    </div>
  </div>
  <div class="searchContainer w-100" >
    <form method="get" action="/listings/search" class="d-flex justify-content-center" role="search">
        <input class="form-control me-2 search-input search" type="search" placeholder="Search destination, places..." name="q" autocomplete="on"/>
        <button class="btn btn-outline-danger" type="submit">Search</button>
    </form>
    <div id="suggestions"></div>
  </div>
</nav>